display "graphics"

colors = list("black","blue","red","green","orange","yellow")

box = rect(0,0,50,50)

for t from 1 to 72 do
  move box, t*5, t*5
  rotate box, t*5
  scale box, t/36, -t/24
  fill box, colors[floor(t/6) % len(colors) + 1]
  sleep 1/36
  repaint
end
