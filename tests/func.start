a = list(0,1,2,3)

print len(a)

begin len do
  return "blargh"
end

print len(a)

begin inner2 do
  print "inner2 foo is `foo`"
  foo = foo + 1 ; refers to the foo in inner1
end

begin inner1(foo) do
  print "inner1 foo is `foo`"
  inner2
  print "inner1 foo is `foo`"
  foo = foo + 1
  print "inner1 foo is `foo`"
end

foo = 123
print "outer foo is `foo`"

inner1 42
print "outer foo is still `foo`"

begin compare(first, second) do
  if first > second then
    return 1
  else if first < second then
    return -1
  else
    return 0
  end
end

for x from 8 to 13 do
  print "compare(`x`, 10) is `compare(x, 10)`"
end

begin func1(i) do
  while i < 60 do i = i * 2
  return i
end

begin func2(i) do
  j = i
  while j < func1(i) do j = j + 11
  return j
end

print "func2(10) = `func2(10)` = 87"
